# Database Configuration
DATABASE_URL="postgresql://username:password@host:5432/db?sslmode=require"

# Auth / NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"
AUTH_SECRET="replace-with-random-secret"

# Vercel Blob
BLOB_READ_WRITE_TOKEN="replace-with-blob-token"

# Optional OAuth (if used)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Optional Email Config (if used)
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""

# Optional JWT Secret (if used)
JWT_SECRET=""

# Application Configuration
NEXT_PUBLIC_APP_NAME="Sistem Tahfidz"
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Session Configuration
SESSION_MAX_AGE=86400
